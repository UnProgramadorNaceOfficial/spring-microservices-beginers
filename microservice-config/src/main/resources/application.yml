server:
  port: 8888

spring:
  application:
    name: microservice-config
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/configurations